extends ../layout/index

block contenido
    div.py-0
        h1.text-4xl.my-10.font-extrabold.text-center.text-blue BienesRaices.io
        h2.text-center.text-greendark.text-2xl.font-extrabold= pagina

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-red-500' : 'bg-greendark').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= mensaje

        if !error
            a(href="/auth/login" class="pt-4 text-center font-bold text-sm text-light uppercase") Ya puedes Iniciar Sesión