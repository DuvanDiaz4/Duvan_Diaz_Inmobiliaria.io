extends ./layout/app

block contenido 

        section(class="py-5")
            h2(class="text-center text-4xl font-extrabold pt-2 text-greendark")= pagina
            
            if propiedades.length
                div(class="grid grid-cols- lg:grid-cols-3 gap-5 mt-14")
                    each propiedad in propiedades
                        include ./templates/card 

            else 
                p(class="text-center text-greendark text-sm py-10") No Hay Resultados

block footer 
    include ./layout/includes/footer

